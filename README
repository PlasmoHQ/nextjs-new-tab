# NextJS Chrome Extension New Tab Page Starter

Example of Chrome Extension that uses NextJS to render a new tab page.

# Usage

1. `pnpm install`
2. `pnpm run build`
3. Go to Chrome , enable Developer Mode, and load the `extensions/` folder as unpacked

# Made by

[Plasmo](https://www.plasmo.com) -- We build dev tooling for browser extensions!
